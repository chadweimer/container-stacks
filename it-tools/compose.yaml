networks:
  public:
    external: true

services:
  app:
    container_name: it-tools
    image: ghcr.io/corentinth/it-tools:${IMAGE_TAG:-latest}
    labels:
      homepage.group: Other
      homepage.href: https://${HOST}
      homepage.icon: it-tools.png
      homepage.name: IT-Tools
      traefik.enable: true
      traefik.http.routers.it-tools.rule: Host(`${HOST}`)
      traefik.http.routers.it-tools.tls.certresolver: myresolver
      traefik.http.routers.it-tools.tls.domains[0].main: ${TLS_CERT_DOMAIN:-${HOST}}
      traefik.http.routers.it-tools.tls.domains[0].sans: ${TLS_CERT_SANS}
    networks:
      - public
    pull_policy: always
    restart: unless-stopped
